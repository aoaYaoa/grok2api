# Upstream v3.1.4 Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Synchronize `chenyme/grok2api` through `909bb8109fcc56b34612066630f5c0bf4e43e517` while preserving the local React public workspaces, media cache, video recovery, SQLite deployment, and multi-WARP routing.

**Architecture:** Apply the cumulative upstream tree delta from the recorded marker `f42ba1765fa520c6a587387daf8b22612168b397` into the vendored `gateway/` prefix with Git three-way patching. Resolve conflicts by combining upstream v3.1.4 reliability and management features with local public media workflows and production contracts, then verify the complete Go, React, and Quality Guard suites before deployment.

**Tech Stack:** Go 1.26, React 19, TypeScript 6, Vite 8, pnpm 11, SQLite, Docker Compose.

## Global Constraints

- Do not modify `xianyuandaxian` or OpenClaw.
- Do not add Redis or replace SQLite/memory runtime deployment.
- Preserve local Chat, Imagine, image edit, Video, NSFW, Voice, cache, media playback, extension, five-minute retry, and cross-account task behavior.
- Preserve Docker volumes, media files, tasks, credentials, server configuration, and all existing WARP containers.
- Deploy only `grok2api_go`; never run Compose `down` or recreate WARP services.
- Record the exact upstream commit and import date in `gateway/UPSTREAM.md`.

---

### Task 1: Import the cumulative upstream delta

**Files:**
- Modify: upstream-changed files under `gateway/`
- Modify: `gateway/UPSTREAM.md`

**Interfaces:**
- Consumes: marker `f42ba1765fa520c6a587387daf8b22612168b397`
- Produces: upstream v3.1.4 changes under the local `gateway/` prefix

- [ ] **Step 1: Generate and apply the cumulative patch**

```bash
git diff --binary --full-index f42ba1765fa520c6a587387daf8b22612168b397 909bb8109fcc56b34612066630f5c0bf4e43e517 > /tmp/grok2api-upstream-20260820.patch
git apply --3way --directory=gateway /tmp/grok2api-upstream-20260820.patch
```

- [ ] **Step 2: Inventory conflicts and new files**

```bash
git diff --name-only --diff-filter=U
rg -n '^(<<<<<<<|=======|>>>>>>>)' gateway
```

### Task 2: Integrate backend reliability and account changes

**Files:**
- Modify: `gateway/backend/internal/application/account/**`
- Modify: `gateway/backend/internal/application/gateway/**`
- Modify: `gateway/backend/internal/infra/provider/**`
- Modify: `gateway/backend/internal/transport/http/inference/**`
- Test: matching Go test files

**Interfaces:**
- Consumes: upstream refresh-token import, empty-stream retry, protocol-safe terminal events, missing-thinking quality retry, compaction compatibility, and trusted-client-IP metadata
- Produces: current upstream reliability without removing local media execution contracts

- [ ] **Step 1: Preserve local media retry and task ownership behavior while integrating upstream request-quality retry**
- [ ] **Step 2: Integrate Build refresh-token persistence and import recovery**
- [ ] **Step 3: Integrate Responses compaction and Grok CLI serialization fixes**
- [ ] **Step 4: Run focused gateway, account, CLI, conversation, and inference tests**

```bash
go test ./internal/application/account ./internal/application/gateway ./internal/infra/provider/cli ./internal/infra/provider/conversation ./internal/transport/http/inference
```

### Task 3: Integrate egress, persistence, audit, and admin UI changes

**Files:**
- Modify: `gateway/backend/internal/application/egress/**`
- Modify: `gateway/backend/internal/infra/persistence/relational/**`
- Modify: `gateway/frontend/src/features/accounts/**`
- Modify: `gateway/frontend/src/features/audits/**`
- Modify: `gateway/frontend/src/features/quality-guard/**`
- Modify: `gateway/frontend/src/features/settings/**`

**Interfaces:**
- Consumes: shared proxy profiles, bounded auto-assignment, durable quality penalties, reasoning-effort audit fields, and account status breakdowns
- Produces: updated management features while retaining all local public React routes and styling

- [ ] **Step 1: Integrate proxy profile schema, repository, API, and admin controls**
- [ ] **Step 2: Integrate durable quality state, audit metadata, and quota/account presentation**
- [ ] **Step 3: Verify no upstream operation replaces files under `gateway/frontend/src/public/`**
- [ ] **Step 4: Run frontend tests, lint, and both builds**

```bash
pnpm install --frozen-lockfile
pnpm test
pnpm lint
pnpm build
```

### Task 4: Verify, publish, and deploy

**Files:**
- Modify: `gateway/UPSTREAM.md`
- Add: `docs/superpowers/plans/2026-08-20-upstream-v3.1.4-sync-plan.md`

**Interfaces:**
- Consumes: fully merged and tested source tree
- Produces: synchronized `origin/main` and healthy production application container

- [ ] **Step 1: Record `909bb8109fcc56b34612066630f5c0bf4e43e517`, version `v3.1.4`, and import date `2026-08-20`**
- [ ] **Step 2: Run complete verification**

```bash
cd gateway/backend && go test ./... && go build -o /tmp/grok2api-sync-20260820 ./cmd/grok2api
cd ../frontend && pnpm test && pnpm lint && pnpm build
cd ../tools/egress-quality-guard && python3 -m unittest quality_guard_test.py session_rotator_test.py
git diff --check
```

- [ ] **Step 3: Commit and push**

```bash
git add -A
git commit -m "sync upstream 909bb810"
git push origin HEAD:main
```

- [ ] **Step 4: Rebuild only production `grok2api_go`**

```bash
ssh netcup 'cd /root/grok2api-go-release && git fetch origin main && git checkout --detach origin/main && docker compose -f docker-compose.yml -f docker-compose.multi-warp.yml build grok2api_go && docker compose -f docker-compose.yml -f docker-compose.multi-warp.yml up -d --force-recreate --no-deps grok2api_go'
```

- [ ] **Step 5: Verify health, commit, volume mounts, and existing WARP containers**

```bash
curl -fsS https://grok.uonoe.com/healthz
ssh netcup 'docker ps --filter name=grok2api-go-release --format "{{.Names}}\t{{.Status}}"'
```
