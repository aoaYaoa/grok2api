# Upstream v3.0.7 Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Selectively synchronize `chenyme/main` at `11bb5e2` into the customized gateway without replacing local React workspaces, media cache, cross-account routing, WARP, SQLite, or memory runtime behavior.

**Architecture:** Treat upstream `4d42f08..11bb5e2` as a reference patch. Apply clean backend changes under `gateway/backend`, resolve divergent gateway and Web media files against local behavior, then adapt only required admin account and egress UI fields under `gateway/frontend`.

**Tech Stack:** Go 1.26, Gin, GORM/SQLite, React 19, TypeScript, Vite, Docker Compose.

---

### Task 1: Import Backend Reliability Changes

**Files:**
- Modify: `gateway/backend/internal/application/{account,egress,gateway}`
- Modify: `gateway/backend/internal/domain/{account,egress}`
- Modify: `gateway/backend/internal/infra/{egress,persistence,provider}`
- Modify: `gateway/backend/internal/transport/http/{egress,inference}`
- Modify: `gateway/VERSION`

- [ ] Apply the clean `4d42f08..11bb5e2` backend patch under `gateway/`.
- [ ] Resolve divergent gateway and Web media files while preserving local five-minute limits, cross-account retry, response ownership, and public error sanitization.
- [ ] Keep upstream regression tests for upload diagnostics, video response logging, prompt-cache affinity, proxy-pool cooldown isolation, and billing inference.
- [ ] Run focused changed-package tests.

### Task 2: Adapt Admin UI

**Files:**
- Modify: `gateway/frontend/src/features/settings/{egress-nodes.tsx,settings-api.ts}`
- Modify: `gateway/frontend/src/features/accounts/{account-quota.tsx,accounts-page.tsx}`
- Modify: `gateway/frontend/src/shared/i18n/index.ts`
- Test: `gateway/frontend/test/gateway-path-contract.test.mjs`

- [ ] Add proxy-pool DTO and controls using existing compact form patterns.
- [ ] Preserve the local `Imagine` remaining percentage and recovery-time account state while adding upstream billing presentation fixes.
- [ ] Keep the customized video gallery instead of replacing it with upstream layout code.
- [ ] Run frontend contract tests.

### Task 3: Verify And Deploy

**Files:**
- Modify: `gateway/UPSTREAM.md`

- [ ] Run changed-package and full Go tests, vet, and build.
- [ ] Run dependency-independent frontend tests.
- [ ] Record upstream `11bb5e2` and `v3.0.7`, commit, and push `HEAD:main`.
- [ ] Build and force-recreate only production `grok2api_go`; verify health, `/gateway/accounts`, WARP, SQLite data volume, and no Redis service.
