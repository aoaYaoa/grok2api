# Upstream v3.0.9 Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate `chenyme/grok2api` through `d2a8b4f7` into the customized `gateway/` tree while preserving all local public workspaces, media workflows, SQLite deployment, memory runtime, and WARP topology.

**Architecture:** Generate one path-mapped three-way patch from the recorded upstream base `dae50ce6` to `d2a8b4f7`, applying upstream `backend/`, `frontend/`, `VERSION`, and `config.example.yaml` beneath `gateway/`. Resolve conflicts in favor of upstream reliability and management features while retaining local public React pages, media caching, video extension behavior, anti-bot integration, and deployment files.

**Tech Stack:** Go 1.26, React 19, TypeScript 6, Vite 8, SQLite, Docker Compose, WARP

---

### Task 1: Apply The Path-Mapped Upstream Patch

**Files:**
- Modify: `gateway/backend/**`
- Modify: `gateway/frontend/**`
- Modify: `gateway/VERSION`
- Modify: `gateway/config.example.yaml`

- [ ] **Step 1: Generate the exact upstream patch**

Run:

```bash
git diff --binary dae50ce6 d2a8b4f7 -- VERSION config.example.yaml backend frontend --output=/tmp/grok2api-upstream-v309.patch
```

Expected: `/tmp/grok2api-upstream-v309.patch` contains only upstream application paths.

- [ ] **Step 2: Apply it beneath `gateway/` with three-way conflict support**

Run:

```bash
git apply --3way --index --directory=gateway /tmp/grok2api-upstream-v309.patch
```

Expected: cleanly merged files are staged; divergent local files are left with normal Git conflict stages.

- [ ] **Step 3: Confirm deployment and public workspace paths were not imported**

Run:

```bash
git status --short
git diff --name-only -- docker-compose.yml docker-compose.multi-warp.yml gateway/frontend/src/public
```

Expected: no deployment file changes and no upstream replacement of `gateway/frontend/src/public/**`.

### Task 2: Resolve Backend Reliability Changes

**Files:**
- Modify: `gateway/backend/internal/application/account/**`
- Modify: `gateway/backend/internal/application/gateway/**`
- Modify: `gateway/backend/internal/application/model/**`
- Modify: `gateway/backend/internal/infra/egress/**`
- Modify: `gateway/backend/internal/infra/persistence/relational/**`
- Modify: `gateway/backend/internal/infra/provider/**`
- Modify: `gateway/backend/internal/repository/**`
- Modify: `gateway/backend/internal/transport/http/account/**`

- [ ] **Step 1: Integrate account and session invalidation**

Preserve upstream blocked-account classification from `provider/account_block.go`, session block precedence, chat 403 SSO invalidation, and quota blocked-user handling. Keep local account eligibility, quota presentation, cross-account video ownership, and media-job rules.

- [ ] **Step 2: Integrate route-aware selection**

Carry `modelRouteID` through candidate listing and segmented selection so multiple public IDs may map to one upstream model without sharing the wrong model-specific cooldown. Preserve local sticky routing, WARP affinity, capability filters, and SQLite repository contracts.

- [ ] **Step 3: Integrate model-route schema relaxation**

Remove only the obsolete provider/upstream uniqueness constraint while retaining globally unique `public_id`, idempotent SQLite migration, and all local schema upgrades.

- [ ] **Step 4: Integrate CLI and Console protocol fixes**

Bring in Build `0.2.111`, nullable tool-root normalization, generated tool declarations, OAuth fixes, Console nullable handling, and `webVisibleStreamPhase`. Preserve local provider adapters and response/media archiving.

- [ ] **Step 5: Run focused backend tests**

Run:

```bash
go test ./internal/application/account ./internal/application/gateway ./internal/application/model ./internal/infra/egress ./internal/infra/persistence/relational ./internal/infra/provider/... ./internal/transport/http/account
```

Expected: all selected packages pass.

### Task 3: Integrate Management UI Features

**Files:**
- Modify: `gateway/frontend/src/features/accounts/accounts-api.ts`
- Modify: `gateway/frontend/src/features/accounts/accounts-page.tsx`
- Modify: `gateway/frontend/src/features/creative-console/creative-console-page.tsx`
- Modify: `gateway/frontend/src/shared/i18n/index.ts`

- [ ] **Step 1: Add Build quota reset operations**

Integrate the upstream batch quota reset API and controls using the existing account table, selection state, dialogs, theme tokens, and mobile layout.

- [ ] **Step 2: Add updated egress binding controls**

Integrate upstream proxy binding selection without replacing local account quota/status presentation or customized account actions.

- [ ] **Step 3: Add Creative Console message operations**

Integrate regenerate, edit, delete, and truncation behavior while keeping the existing Console provider selection and application shell.

- [ ] **Step 4: Preserve public React applications**

Run:

```bash
node --test test/public-app-contract.test.mjs
```

Expected: all public route, cache, media, layout, and theme contracts pass.

### Task 4: Record And Verify The Sync

**Files:**
- Modify: `gateway/UPSTREAM.md`
- Modify: `gateway/VERSION`

- [ ] **Step 1: Record the upstream revision**

Set `gateway/UPSTREAM.md` to commit `d2a8b4f743562d8a162f14aadf060f1e6f51faa3` and import date `2026-07-25`; describe v3.0.9 reliability, routing, quota reset, egress binding, Console, and stream-phase changes.

- [ ] **Step 2: Run complete backend verification**

Run from `gateway/backend`:

```bash
go test ./...
go vet ./...
go build ./cmd/grok2api
```

Expected: all commands exit successfully.

- [ ] **Step 3: Run complete frontend verification**

Run from `gateway/frontend`:

```bash
pnpm lint
pnpm build
node --test test/public-app-contract.test.mjs
```

Expected: build and tests pass; lint has no errors.

- [ ] **Step 4: Verify deployment configuration and patch hygiene**

Run from the repository root:

```bash
docker compose -f docker-compose.yml -f docker-compose.multi-warp.yml config --quiet
git diff --check
git status --short
```

Expected: Compose renders, whitespace checks pass, and only reviewed sync files plus this plan are changed.

### Task 5: Publish And Deploy

**Files:**
- No additional source files.

- [ ] **Step 1: Commit and update `main`**

Commit the reviewed sync on `codex/upstream-v309-sync-20260725`, then fast-forward `main` to that commit and push `origin/main`.

- [ ] **Step 2: Deploy only the Go application**

Fetch the pushed commit in `/root/grok2api-go-release`, rebuild `grok2api_go`, and force-recreate only that service. Do not run `docker compose down`, recreate WARP, add Redis, or modify SQLite/media volumes.

- [ ] **Step 3: Verify production**

Confirm the production Git SHA, container image ID, healthy status, local `/healthz`, startup reconciliation, account quota catch-up, and absence of new startup errors. Do not run paid media generation as part of deployment verification.
